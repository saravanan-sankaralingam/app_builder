# Parked builder components

Components in this folder are **not currently imported** anywhere. They are saved here for future use, typically when their Build-mode home is ready.

## ViewBuilder.tsx

A two-pane editor for a Data Form view: live table preview on the left, config sidebar on the right (Settings/Style tabs) with Columns, Field Permissions, Data Filter, Quick Filter, Bulk Action, Default Sort sections, plus a Field Permissions modal.

**Why parked:** Spec X and Spec Y are readable specification documents (prose-first), not configuration editors. The ViewBuilder belongs in the Build mode editor surface, which will pick it up later. Built 2026-05-20, parked 2026-05-20.
